import { useNavigation } from "@react-navigation/native";
import { Text } from "react-native-paper";

export default function AddDevice() {

  const navigation = useNavigation();

  return (
    <Text>Add device page </Text>
  )
}
